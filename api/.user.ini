; Upload limits for hosts running PHP as FPM/CGI, where php_value in .htaccess is
; ignored. Harmless on hosts that use mod_php instead — the .htaccess wins there.
upload_max_filesize = 32M
post_max_size = 34M
max_execution_time = 120
