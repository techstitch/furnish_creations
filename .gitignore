# Local development overrides — never committed, never uploaded.
api/config.local.php

# Written at runtime on the server.
data/content.json
data/sessions/
Assets/uploads/*
!Assets/uploads/.htaccess

# Real config holds the DB password and admin hash — template only is tracked.
api/config.php
